# Copy this file to `.env` (do not commit `.env`) and adjust values as needed.

# Backend API base URL used by `src/services/api.ts`
# Example for local NestJS API:
# VITE_API_BASE_URL=http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000
